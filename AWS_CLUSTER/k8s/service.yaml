---
apiVersion: v1
kind: Service
metadata:
    name: website
    namespace: kubernetes-github-actions
    labels:
        app: website
spec:
    type: LoadBalancer
    selector:
        app: website
    ports:
    - port: 81
      protocol: TCP  
      targetPort: 82
